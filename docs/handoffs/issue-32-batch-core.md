# Issue #32 格式统一批量 Core

来源 #32；依赖 #34/#31 已落地主线；Owner 本轮授权连续实现、审核、合并和验证。

实现前用例（初始 NOT RUN）：FT32-01 多文件混合三格式，共用规则，逐个同格式输出；FT32-02 每文件不同工作表名/表头行；FT32-03 中间失败后继续且早先成功文件保留；FT32-04 全部失败逐项原因；FT32-05 任一输出不可覆盖整批任一输入，已有输出未确认保护；FT32-06 重复目标冲突不覆盖先前结果；FT32-07 取消不回滚已提交结果。独立 Core 功能运行与 xUnit 回归分开记录。

契约：批量列表每项包含 WorksheetSource、输出路径、覆盖许可，整个请求只有一套规则。输出路径由调用方按记忆目录生成；逐项返回原顺序和成功/错误。Core 顺序执行，每项失败继续；取消终止余下工作，已写结果不回滚。批内输出重名全部冲突项失败，不能通过覆盖许可吞掉另一项结果。

验证：Release build 0 warnings/errors；250 Core + 16 App 回归全部 PASS。独立 tools/Issue32.FunctionalQA 实际执行 FT32-01～07 PASS，三格式结果通过各自 reader 重开验证；包含不同 Sheet/表头、中间失败、全部失败、输入保护、输出重名、取消保留。Core Functional QA PASS；不代表 WPF 人工 QA。
