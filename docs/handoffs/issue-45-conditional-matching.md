# Issue #45 条件匹配实现与验证

来源 #45 / #43，关联 Bug #41。Owner 本次明确由 Codex 独立完成并审核继续，无跨角色交接。
基线 main：3905e3b9406865d7bae73df0dfd2e90ad19768a6。

## 已完成

- Core 支持可选主表列等于文本值的筛选，先判参与条件再按现有联合 AND 匹配。
- 未参与行保留原位置和字段，返回字段留空，状态未参与匹配，SkippedCount 独立计数。
- UI 第 4 区提供开关、主表列与比较值；默认关闭，值预填是，修改主表清除筛选列；成功后展示跳过数量。
- 请求与 Summary 使用新增 init 属性，保留旧构造调用；更新处理规则、契约、UI 和架构。
- 不更改 #34 多格式 I/O 或依赖库。先交付当前已实现的 .xlsx 路径，#34 需随后同步主线进行多格式集成。

## 验证证据

- build PASS，0 warnings / 0 errors。
- test PASS：214 Core + 4 App = 218。
- 新增 Core 11 项执行覆盖联合键、各种行结果、开关、标准化、无效列/值、公式及全部跳过；App 额外用例直接串联真实 ViewModel/Core，验证筛选参数传递、统计、关闭与主表切换。
- 既有文件占用、成功重试、覆盖取消/另存测试继续通过。
- `dotnet run --project tools/Issue45.FunctionalQA -- <新输出目录>` 独立 Core 功能运行通过，生成两个合成样例、结果与逐步人工操作说明。
- Core 功能样例：总计7，成功1、未匹配1、重复1、空键1、未参与3；返回两列，原两输入文件字节不变。
- diff 检查与范围自审通过；不提交构建产物、样例或缓存。

## 未完成与验收边界

- WPF/WPS 人工 Functional QA：NOT RUN。没有启用桌面 GUI 自动化，不将上述自动化写为 UI QA PASS。
- FT-14 的 xls/csv：等待 #34/#31 集成，当前功能不实现它们的文件读写或选择。
- 合并后必须在最新 main 重跑 build/test 与 Core 功能运行，并生成候选程序供实际桌面验收。
- 发布包验收仍须对应 exact filename + size + SHA256，不把构建成功当作最终验收。
- 本 Issue 保持等待 Functional QA，不以代码合并自动声明 Done。
