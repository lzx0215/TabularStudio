# Issue #52 批量反馈修复与验证

来源：Owner 2026-09-08 四项使用反馈，GitHub #52。更新已确认需求 §14 与 UI 规格；修复 #33 交互，不改 Core 算法/契约/匹配能力。沿用本轮独立完成与验证授权。

## 变更

1. 添加对话框仍支持多选，但改为追加；拖拽同路径；路径去重。
2. 行勾选用于修改/移除，逐个替换取消保留，新文件重建配置；每行预览按钮及独立预览下拉提供明确切换入口。
3. 执行区直接显示失败列表，保留 Core Error.Detail 及文件/工作表/表头/输出上下文，不猜错误行列。
4. 无可选规则时 CanStart=false 且 StartAsync 不调用 Core，勾选变化通知按钮与提示。

## 验证

- Release build：PASS，0 warnings/errors，隔离产物目录 artifacts/issue52-build，不覆盖用户正在运行的旧程序。
- 回归：四个新增反馈测试；既有批量测试同步已确认零规则禁用语义；真实缺失文件失败提示断言。
- WPF 组件：不显示窗口、不操作桌面；实际 ComboBox 选择第二/第一文件，验证 DataGrid 数据及列标题随之切换。
- 组件测试初次 FAIL：测试没有安装 DispatcherSynchronizationContext，异步加载在非调度线程修改绑定集合。修正测试调度环境后 App 25/25 PASS；没有掩盖或跳过失败。
- 独立集成：真实 ViewModel/Core 三格式批量→CSV 结果→条件匹配 XLS→CSV 输出及重试 PASS；输入哈希不变。
- 真正的系统文件对话框、鼠标拖拽、WPS 交互未自动执行；最终启动与窗口响应单独记录，不等于完整人工验收。

RT-01/02/04/05 的 ViewModel 回归 PASS；RT-03 的实际 WPF 组件绑定回归 PASS；RT-06 构建和资源布局 PASS。实际桌面人工部分仍 NOT RUN。无契约修改，无跨角色交接。
