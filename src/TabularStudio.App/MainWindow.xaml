<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:TabularStudio.App.ViewModels" xmlns:views="clr-namespace:TabularStudio.App.Views" x:Class="TabularStudio.App.MainWindow" mc:Ignorable="d" Title="TabularStudio - 本地离线表格工具" Icon="Assets/AppIcon.ico" Width="1280" Height="720" MinWidth="960" MinHeight="640" WindowStartupLocation="CenterScreen" Background="{StaticResource Desktop.BackgroundBrush}" FontFamily="{StaticResource AppFontFamily}" FontSize="14" UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:TableComparisonViewModel}">
            <views:TableComparisonView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:BatchFormatViewModel}">
            <views:BatchFormatView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FormatStandardizationViewModel}">
            <views:FormatStandardizationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DataMatchingViewModel}">
            <views:DataMatchingView/>
        </DataTemplate>
    </Window.Resources>
    <Grid Background="{StaticResource Desktop.BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{StaticResource Desktop.BackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,20,0" VerticalAlignment="Center">
                <Image Source="Assets/AppLogo.png" Width="28" Height="28" Stretch="Uniform" Margin="0,0,8,0"/>
                <TextBlock Text="TabularStudio" FontFamily="{StaticResource Desktop.FontFamily}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Desktop.TextBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch" HorizontalAlignment="Left">
                <RadioButton Style="{StaticResource Desktop.TopNavItemStyle}" IsChecked="{Binding IsFormatStandardizationSelected, Mode=TwoWay}" Command="{Binding SelectFormatStandardizationCommand}" AutomationProperties.Name="格式统一">
                <StackPanel Orientation="Horizontal">
                    <Path Style="{StaticResource Desktop.NavGlyphStyle}" Data="{StaticResource IconFormat}" Margin="0,0,10,0"/>
                    <TextBlock Text="格式统一" VerticalAlignment="Center"/>
                </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource Desktop.TopNavItemStyle}" IsChecked="{Binding IsDataMatchingSelected, Mode=TwoWay}" Command="{Binding SelectDataMatchingCommand}" AutomationProperties.Name="数据匹配">
                <StackPanel Orientation="Horizontal">
                    <Path Style="{StaticResource Desktop.NavGlyphStyle}" Data="{StaticResource IconMatch}" Margin="0,0,10,0"/>
                    <TextBlock Text="数据匹配" VerticalAlignment="Center"/>
                </StackPanel>
                </RadioButton>
                <RadioButton Style="{StaticResource Desktop.TopNavItemStyle}" IsChecked="{Binding IsTableComparisonSelected, Mode=TwoWay}" Command="{Binding SelectTableComparisonCommand}" AutomationProperties.Name="表格对比">
                <StackPanel Orientation="Horizontal">
                    <Path Style="{StaticResource Desktop.NavGlyphStyle}" Data="{StaticResource IconCompare}" Margin="0,0,10,0"/>
                    <TextBlock Text="表格对比" VerticalAlignment="Center"/>
                </StackPanel>
                </RadioButton>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="16,0,16,0" VerticalAlignment="Center" HorizontalAlignment="Right" AutomationProperties.Name="完全离线">
                <Ellipse Width="8" Height="8" Fill="{StaticResource Desktop.SuccessBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="完全离线" Foreground="{StaticResource Desktop.SuccessBrush}" FontFamily="{StaticResource Desktop.FontFamily}" FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Background="{StaticResource Desktop.SurfaceBrush}" BorderBrush="{StaticResource Desktop.BorderBrush}" BorderThickness="0,1,0,0">
            <ContentControl Content="{Binding CurrentViewViewModel}"/>
        </Border>
        <DockPanel Grid.Row="2" Margin="16,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource Desktop.SecondaryTextBrush}" FontFamily="{StaticResource Desktop.FontFamily}" FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
        </DockPanel>
    </Grid>
</Window>
