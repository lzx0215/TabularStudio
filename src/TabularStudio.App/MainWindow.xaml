<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:TabularStudio.App.ViewModels" xmlns:views="clr-namespace:TabularStudio.App.Views" x:Class="TabularStudio.App.MainWindow" mc:Ignorable="d" Title="TabularStudio - 本地离线表格工具" Width="1280" Height="720" MinWidth="960" MinHeight="640" WindowStartupLocation="CenterScreen" Background="{StaticResource AppBackgroundBrush}" FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="13">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:BatchFormatViewModel}">
            <views:BatchFormatView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FormatStandardizationViewModel}">
            <views:FormatStandardizationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DataMatchingViewModel}">
            <views:DataMatchingView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="12,4">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Margin="0,0,40,0" VerticalAlignment="Center">
                    <TextBlock Text="TabularStudio" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>

                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton Content="格式统一" Style="{StaticResource NavButtonStyle}" IsChecked="{Binding IsFormatStandardizationSelected, Mode=TwoWay}" Command="{Binding SelectFormatStandardizationCommand}" Margin="0,0,8,0"/>
                    <RadioButton Content="数据匹配" Style="{StaticResource NavButtonStyle}" IsChecked="{Binding IsDataMatchingSelected, Mode=TwoWay}" Command="{Binding SelectDataMatchingCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewViewModel}"/>
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Text="离线环境已就绪" Foreground="#047857" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
