<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="DesktopTokens.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="Desktop.FocusStyle">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="-2" Stroke="{StaticResource Desktop.PrimaryBrush}" StrokeThickness="2" RadiusX="4" RadiusY="4" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.ButtonBaseStyle" TargetType="Button">
        <Setter Property="Height" Value="{StaticResource Desktop.ControlHeight}" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="MinWidth" Value="{StaticResource Desktop.ButtonMinWidth}" />
        <Setter Property="Padding" Value="{StaticResource Desktop.ButtonPadding}" />
        <Setter Property="FontFamily" Value="{StaticResource Desktop.FontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource Desktop.BodySize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Background" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Desktop.ControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource Desktop.FocusStyle}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{StaticResource Desktop.ControlRadius}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Desktop.HoverBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource Desktop.PressedBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource Desktop.DisabledBrush}" />
                <Setter Property="Foreground" Value="{StaticResource Desktop.DisabledTextBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource Desktop.BorderBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="Desktop.SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Desktop.ButtonBaseStyle}" />
    <Style x:Key="Desktop.PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Desktop.ButtonBaseStyle}">
        <Setter Property="Width" Value="{StaticResource Desktop.ExecuteWidth}" />
        <Setter Property="Background" Value="{StaticResource Desktop.PrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Desktop.PrimaryHoverBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource Desktop.PrimaryPressedBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource Desktop.DisabledBrush}" />
                <Setter Property="Foreground" Value="{StaticResource Desktop.DisabledTextBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="Desktop.CompactSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Desktop.SecondaryButtonStyle}">
        <Setter Property="Height" Value="{StaticResource Desktop.CompactHeight}" />
        <Setter Property="MinWidth" Value="{StaticResource Desktop.CompactButtonMinWidth}" />
        <Setter Property="Padding" Value="{StaticResource Desktop.ControlPadding}" />
    </Style>
    <Style x:Key="Desktop.IconButtonStyle" TargetType="Button" BasedOn="{StaticResource Desktop.SecondaryButtonStyle}">
        <Setter Property="Width" Value="{StaticResource Desktop.ControlHeight}" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>
    <Style x:Key="Desktop.TextBoxStyle" TargetType="TextBox">
        <Setter Property="Height" Value="{StaticResource Desktop.ControlHeight}" />
        <Setter Property="Padding" Value="{StaticResource Desktop.ControlPadding}" />
        <Setter Property="FontSize" Value="{StaticResource Desktop.BodySize}" />
        <Setter Property="Background" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Desktop.ControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Frame" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{StaticResource Desktop.ControlRadius}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="Background" Value="{StaticResource Desktop.SubtleBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Desktop.DisabledTextBrush}" />
                            <Setter TargetName="Frame" Property="Background" Value="{StaticResource Desktop.DisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.ComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="{StaticResource Desktop.BodySize}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="Transparent" CornerRadius="{StaticResource Desktop.ControlRadius}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Desktop.SelectedBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Desktop.SelectedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Desktop.DisabledTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.ComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Height" Value="{StaticResource Desktop.ControlHeight}" />
        <Setter Property="Padding" Value="{StaticResource Desktop.ControlPadding}" />
        <Setter Property="FontSize" Value="{StaticResource Desktop.BodySize}" />
        <Setter Property="Background" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Desktop.ControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="MaxDropDownHeight" Value="280" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource Desktop.FocusStyle}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource Desktop.ComboBoxItemStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="Frame" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{StaticResource Desktop.ControlRadius}" SnapsToDevicePixels="True" />
                        <ToggleButton Focusable="False" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" ClickMode="Press" Background="Transparent">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent" />
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Grid Margin="0" IsHitTestVisible="False">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="28" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Margin="8,0,4,0" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" VerticalAlignment="Center" />
                            <Path Grid.Column="1" Data="{StaticResource IconChevronDownLine}" Stroke="{StaticResource Desktop.SecondaryTextBrush}" StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="10" Height="6" Fill="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                            <Border Background="{StaticResource Desktop.SurfaceBrush}" BorderBrush="{StaticResource Desktop.BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource Desktop.ControlRadius}" Padding="4" Margin="0,4,0,0" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True">
                                <ScrollViewer CanContentScroll="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Desktop.DisabledTextBrush}" />
                            <Setter TargetName="Frame" Property="Background" Value="{StaticResource Desktop.DisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.CheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="FontSize" Value="{StaticResource Desktop.BodySize}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource Desktop.FocusStyle}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinHeight" Value="{StaticResource Desktop.ControlHeight}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Box" Width="16" Height="16" CornerRadius="{StaticResource Desktop.ControlRadius}" Background="{StaticResource Desktop.SurfaceBrush}" BorderBrush="{StaticResource Desktop.ControlBorderBrush}" BorderThickness="1" VerticalAlignment="Center" SnapsToDevicePixels="True">
                            <Path x:Name="Mark" Data="{StaticResource IconCheck}" Fill="{StaticResource Desktop.SurfaceBrush}" Stretch="Uniform" Width="10" Height="10" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ContentPresenter Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource Desktop.PrimaryBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                            <Setter TargetName="Mark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                            <Setter TargetName="Box" Property="BorderThickness" Value="2" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Desktop.DisabledTextBrush}" />
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource Desktop.DisabledBrush}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Desktop.BorderBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.ListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    </Style>
    <Style x:Key="Desktop.ListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,4" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource Desktop.FocusStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd" Background="Transparent" CornerRadius="{StaticResource Desktop.ControlRadius}" SnapsToDevicePixels="True">
                        <Grid>
                            <Border x:Name="Accent" Width="2" HorizontalAlignment="Left" Background="Transparent" CornerRadius="1" Margin="0,6" />
                            <ContentPresenter />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#F3F5F0" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Desktop.SelectedBrush}" />
                            <Setter TargetName="Accent" Property="Background" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                            <Setter TargetName="Bd" Property="BorderThickness" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.DataGridHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Desktop.HeaderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="{StaticResource Desktop.ControlPadding}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{StaticResource Desktop.BorderBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.DataGridCellStyle" TargetType="DataGridCell">
        <Setter Property="Padding" Value="{StaticResource Desktop.ControlPadding}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border x:Name="CellBorder" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="CellBorder" Property="Background" Value="{StaticResource Desktop.SelectedBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="CellBorder" Property="BorderBrush" Value="{StaticResource Desktop.PrimaryBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.DataGridStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource Desktop.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowHeight" Value="28" />
        <Setter Property="ColumnHeaderHeight" Value="32" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Desktop.DividerBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Desktop.DividerBrush}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Setter Property="RowBackground" Value="{StaticResource Desktop.SurfaceBrush}" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="EnableRowVirtualization" Value="True" />
        <Setter Property="EnableColumnVirtualization" Value="True" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
        <Setter Property="SelectionMode" Value="Extended" />
        <Setter Property="SelectionUnit" Value="FullRow" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="CellStyle" Value="{StaticResource Desktop.DataGridCellStyle}" />
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource Desktop.DataGridHeaderStyle}" />
        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource Desktop.HoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource Desktop.SelectedBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Desktop.ProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="6" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.PrimaryBrush}" />
        <Setter Property="Background" Value="{StaticResource Desktop.DividerBrush}" />
        <Setter Property="BorderThickness" Value="0" />

    </Style>
    <Style x:Key="Desktop.CompactTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource Desktop.TextBoxStyle}">
        <Setter Property="Height" Value="{StaticResource Desktop.CompactHeight}" />
    </Style>
    <Style x:Key="Desktop.CompactComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource Desktop.ComboBoxStyle}">
        <Setter Property="Height" Value="{StaticResource Desktop.CompactHeight}" />
    </Style>
    <Style x:Key="Desktop.ReadOnlyPathStyle" TargetType="TextBox" BasedOn="{StaticResource Desktop.TextBoxStyle}">
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="Foreground" Value="{StaticResource Desktop.TextBrush}" />
    </Style>
    <Style x:Key="Desktop.CopyableMessageStyle" TargetType="TextBox" BasedOn="{StaticResource Desktop.ReadOnlyPathStyle}">
        <Setter Property="Height" Value="Auto" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
    </Style>
</ResourceDictionary>
