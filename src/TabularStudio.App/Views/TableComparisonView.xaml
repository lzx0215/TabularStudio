<UserControl x:Class="TabularStudio.App.Views.TableComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,8">
            <TextBlock Text="表格对比" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="从 A1 开始逐个位置比较，包含表头；忽略样式，保留数据类型、空格和大小写差异。"
                       Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />
        </StackPanel>
        <Grid Grid.Row="1" IsEnabled="{Binding CanConfigure}">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Content="选择文件…" Command="{Binding BrowseLeftCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        <TextBlock Text="表一" Style="{StaticResource SectionHeaderStyle}" VerticalAlignment="Center" />
                    </DockPanel>
                    <TextBox x:Name="LeftFileName" Text="{Binding Left.FileName, Mode=OneWay}" IsReadOnly="True" ToolTip="{Binding Left.FilePath}" Margin="0,8,0,8" />
                    <DockPanel>
                        <TextBlock Text="工作表" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox ItemsSource="{Binding Left.Worksheets}" SelectedItem="{Binding Left.SelectedWorksheet}" DisplayMemberPath="Name" IsEnabled="{Binding Left.CanSelectSheet}" ToolTip="CSV 无需选择工作表" />
                    </DockPanel>
                    <TextBlock Text="{Binding Left.ErrorMessage}" TextWrapping="Wrap" Foreground="{StaticResource DangerBrush}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Content="选择文件…" Command="{Binding BrowseRightCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        <TextBlock Text="表二" Style="{StaticResource SectionHeaderStyle}" VerticalAlignment="Center" />
                    </DockPanel>
                    <TextBox x:Name="RightFileName" Text="{Binding Right.FileName, Mode=OneWay}" IsReadOnly="True" ToolTip="{Binding Right.FilePath}" Margin="0,8,0,8" />
                    <DockPanel>
                        <TextBlock Text="工作表" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox ItemsSource="{Binding Right.Worksheets}" SelectedItem="{Binding Right.SelectedWorksheet}" DisplayMemberPath="Name" IsEnabled="{Binding Right.CanSelectSheet}" ToolTip="CSV 无需选择工作表" />
                    </DockPanel>
                    <TextBlock Text="{Binding Right.ErrorMessage}" TextWrapping="Wrap" Foreground="{StaticResource DangerBrush}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
        </Grid>
        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="公式使用文件中已保存的计算结果，请先重新计算并保存。末尾纯空行列不计入数据范围。" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,10" />
            <StackPanel Orientation="Horizontal">
                <Button Content="开始对比" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                <Button Content="取消" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="10,0,0,0" />
                <Button Content="导出全部差异…" Command="{Binding ExportCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="10,0,0,0" />
            </StackPanel>
            <ProgressBar Height="8" Minimum="0" Maximum="100" Value="{Binding ProgressPercent}" IsIndeterminate="{Binding IsBusy}" Margin="0,8,0,8" />
            <TextBlock x:Name="ResultSummary" Text="{Binding Summary}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock Text="{Binding ExtentHint}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0" />
            <TextBlock Text="{Binding DifferenceHint}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,5,0,0" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,5,0,0" />
        </StackPanel>
        <DataGrid x:Name="DifferencesGrid" Grid.Row="3" ItemsSource="{Binding Differences}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" EnableRowVirtualization="True" EnableColumnVirtualization="True" MinHeight="80">
            <DataGrid.Columns>
                <DataGridTextColumn Header="位置" Binding="{Binding Address}" Width="85" />
                <DataGridTextColumn Header="表一类型" Binding="{Binding LeftType}" Width="75" />
                <DataGridTextColumn Header="表一值" Binding="{Binding LeftValue}" Width="*" />
                <DataGridTextColumn Header="表二类型" Binding="{Binding RightType}" Width="75" />
                <DataGridTextColumn Header="表二值" Binding="{Binding RightValue}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBox Grid.Row="4" Text="{Binding ReportPath, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                 TextWrapping="Wrap" Margin="0,8,0,0" ToolTip="报告已保存的位置（可复制）" />
    </Grid>
</UserControl>
